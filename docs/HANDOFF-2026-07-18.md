# Session Handoff, 2026-07-18 (rounds 9-10: review UX per Dan's verdict, measurement unblocked, consolidation shipped)

> Source of truth for the next session. Read alongside docs/2026-07-11-director-eval-findings.md
> (ADDENDA 8 and 9 + postscript are this session's measured verdicts), docs/TO-VERIFY.md (top two
> sections are Dan's open smoke items), and docs/plans/2026-07-18-001 (round-9 plan).
> Prior context: docs/HANDOFF-2026-07-17.md.

## TL;DR

1. **Round 9 shipped off Dan's first finished-video verdict**: coherent trailing speculation KEPT
   by default (new `speculation` category, prompt v2 + DIRECTOR_PROMPT_VERSION), mid-flow fillers
   unchecked by default (word-gap evidence, SMOOTH_GAP_SEC 0.2), cut-row timestamp click seeks to
   (start - lead-in) and plays with a 1-10s slider, Done button REMOVED (review persists, toggling
   an applied cut restores/re-removes the segment live). All four behaviors live-verified in-app.
2. **The variance round ran (ADDENDUM 8)**: match metric is TIGHT across draws (std 0.2-1.3pts),
   essLost needs 3-run means, the 0.90-bar gaps on google/how-to-edit are STRUCTURAL (span
   discipline ceilings), not noise.
3. **Consolidation measured and SHIPPED (ADDENDUM 9)**: retake+structural default-on, Settings
   toggles deleted (store v4 migration). Match up on all four fixtures, AUTO harm identical.
4. **hermes AUTO harm attributed (A9 postscript)**: per-op essLost attribution (new eval feature)
   shows the DE-DUP family (repeat+redundancy, 33 of 57 words) destroys lines Dan deliberately
   kept (his sign-off catchphrase among them). Round-11 lever: deliberate-repeat protection in the
   near-verbatim AUTO path. Pacing was refuted as the driver.
5. **PR #56 still awaits Dan; PR #57 opened** (feat/director-eval, self-contained rounds 1-10).

## Branch / commit state (all pushed)

feat/director-eval, ~70 commits, PR https://github.com/fullvaluedan/vibecut/pull/57. Suites at
tip: apps/web 1413 pass + 3 pre-existing mask failures, hf-bridge 177/177, tsc clean. Session
commits: 048a8d01 (round-9 U1 + plan), 0ea0d4ba (speculation), 5e9d0bc7 (seek + Done removal),
80c8e1e8/c2eaa2cc (docs + dev hook), merges 3f8e5ba0 (variance aggregation), 7d6b2783 (hygiene
batch: midpoint helper, test envelope, retake/structural version constants, openWith deleted),
6641603e (consolidation), 0c68a62d area (attribution + TO-VERIFY).

## What Dan still owes (his list, docs/TO-VERIFY.md top)

Taste-only: does speculation tagging match his intent on real footage; does the smooth-filler
split match his ear; is 1s default lead-in right; do the always-on recall passes feel like recall
or noise; run-time feel. Everything mechanical was agent-verified in-app this session.

## Hard-won lessons THIS session

- **The in-session browser pane at 0x0 still executes JS**; full smoke recipe (mobile-block
  bypass, text-clip seeding, `window.__directorPlanStore` dev hook, playback ticks/120000) is in
  auto-memory dev-server-cwd-and-port. Real media import is IMPOSSIBLE there (WebCodecs wall).
- **Worktree agents**: explicitly instruct `git reset --hard feat/director-eval` first (all three
  baselined stale); fresh worktrees need `bun install` + `.content-collections` copied from the
  main checkout or gates false-fail; a worktree where an agent ran `next build` cannot be removed
  by git (Filename too long) - robocopy /MIR an empty dir over it first.
- **The eval's --runs salt already existed** (runIndex in the cache key since 4d912e08); only the
  aggregation report was missing. Check before building.
- The eval report piped through `Select-Object -Last N` DESTROYS the earlier fixtures' sections;
  always capture to a file with `*>`.
- Attribution > hypothesis: the "obvious" pacing suspect for hermes was wrong; 30 minutes of
  eval-feature work replaced guessing.

## Next work, recommended order

1. **Dan's smoke pass** on rounds 9-10 (TO-VERIFY top two sections). His taste verdict routes
   round 11.
2. **Deliberate-repeat protection** (the measured lever): a bookend/sign-off signal that demotes
   near-verbatim de-dup groups containing catchphrase-like repeats (same line recurring far apart,
   or at section boundaries) to opt-in, plus a keeper-choice audit against Dan's picks. Target:
   hermes AUTO essLost 57 -> ~25 without match regression. Measure with --runs 3 (essLost needs
   means; match moves >2pts = signal).
3. **Span-discipline / recall levers** for google (62) and how-to-edit (37): their ceilings bind
   before thresholds; needs better offered spans, not tuning.
4. **PR hygiene**: #57 review/merge decision, #56 still open.
5. **Small**: ripple-drag preview still awaits Dan's feel-check (TO-VERIFY); the A/B-preview +
   new-run edge (flip back before re-running) is documented but could auto-redo in run-director.

## Pickup prompt for a new session (copy-paste)

```
You are picking up VibeCut (Dan's AI video editor). Repo: D:\Claude\framecut (git, branch
feat/director-eval, pushed, PR #57). Read docs/HANDOFF-2026-07-18.md FIRST, then
docs/2026-07-11-director-eval-findings.md ADDENDA 8-9 + postscript, docs/TO-VERIFY.md top two
sections.

CONTEXT: Round 9 (Dan's review-UX verdict) and round 10 (variance measurement, consolidation
default-on, essLost attribution) shipped and are agent-verified; Dan's taste pass is pending.
The measured round-11 lever is deliberate-repeat protection in the near-verbatim de-dup AUTO
path (hermes AUTO essLost 57, 58% from repeat+redundancy destroying lines Dan kept on purpose).

FIRST MOVE: ask Dan for his rounds 9-10 taste verdict. Then lever 2 in the handoff's next-work
order. PROCESS: plan doc, commit per unit, gates = suites (1413+3 known / 177) + tsc + diag
assertions + eval (--runs 3 for essLost claims; capture output with *> to a file). Worktree
agents: tell them git reset --hard feat/director-eval, bun install, copy .content-collections.
HARD RULES unchanged: Edit tool only; no em dashes; PATCHES.md same-commit; prompt wording
changes bump the pass's version constant (DIRECTOR/RETAKE/STRUCTURAL/VERIFY all exist now); new
DirectorOpCategory needs the five-piece checklist; never edit pipeline code while an eval runs;
browser-pane smoke recipe is in auto-memory (window.__directorPlanStore).
```
