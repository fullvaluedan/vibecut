# Session handoff — 2026-07-03

> Long session (editor perf + two rounds of AI-CUT recut-quality work). Everything below is committed on `feat/director-importance` and **NOT pushed**. Two units remain, and **nothing has been verified live in the app yet** — that is the biggest open risk.

## Branch / state

- Branch: `feat/director-importance` (do not push without Dan's explicit go).
- Latest commit at handoff: `b9043d67` (2P-U3). Roughly 12 unpushed commits sit on top of origin.
- Verify current state before doing anything: `git -C C:/Users/danom/Videos/framecut-director log --oneline -14` and `git status`.
- **Do NOT use browser automation** in the next session (Dan's instruction — it ate hours of this one). Verify by running AI CUT yourself in the app only if Dan asks, otherwise hand him the app to click.
- Test baseline: `bun test` in `apps/web` shows **3 pre-existing failures** (custom mask point insertion + mask snapping x2). The bun wasm/video-sink init is FLAKY and can spuriously report ~14 fails — re-run once; only the `ai-generate/director` tests being green matters. `bunx tsc --noEmit` should be clean in `apps/web`.

## What shipped today (all committed, green, unpushed)

Two plans drove this:
- `docs/plans/2026-07-02-002-fix-recut-quality-and-timeline-perf-plan.md` (perf U5-U8 + recut U1-U4)
- `docs/plans/2026-07-03-001-fix-second-pass-cleanup-plan.md` (second-pass 2P-U1..U5; note R9 + unit 2P-U5 were added mid-session per Dan's "unnecessary cuts" report)

**Editor performance (the 826-clip lag):**
- U5 coalesce drag to one update/frame + memoize the clip
- U6 precompute A/V-sync offsets once per tracks-change (killed the O(n^2) scan)
- U7 virtualize the timeline to on-screen clips
- U8 prefetch decode across cut boundaries (the ~594ms playback freezes)

**Recut quality round 1:**
- U1 auto-accept high-confidence repeats (verbatim phrase-repeat + >=0.7 redundancy; uncertain band stays opt-in)
- U2 VAD dead-air default-on for the Director path (removes real silence, not just um/uh)
- U3 whole-take deletion (redundant) + NEW out-of-context LLM pass over the full transcript (opt-in rows)
- U4 trim-vs-cut + consolidate adjacent same-source clips (one undo)

**Recut quality round 2 (the sliver/mistake fixes Dan flagged from screenshots):**
- 2P-U1 `e283218b` coalesce sub-floor removal gaps so no <15-frame sliver survives (word-guarded: never swallow a complete content word; fail-open with no transcript)
- 2P-U2 `141a8b4f` word-aware micro-clip sweep for pre-existing shards (floor 5->15 frames; no-content shard auto-removes, content-bearing stays opt-in)
- 2P-U3 `b9043d67` virtual second-pass convergence loop (remap transcript through accepted cuts, re-run deterministic detectors on the compressed result, inverse-map findings back to original coords, dedup, fold into same review; cap 3 passes; one review/one undo)

Earlier context (same branch): the Groq 413 upload-size guard, the transcript-tab hang fix, the emphasis-pause protection fix (`5b7d6efe`), and the edit-by-transcript feature.

## What is NOT done — pick up here

### 2P-U4 (context auto-accept + prompt hardening) — NOT started in code
Plan: `docs/plans/2026-07-03-001-...` unit U4 + KTD6. Dan's revised call: confident out-of-context segments should be REMOVED by default, and the prompt should catch mistake meta-asides.
Findings already gathered (so you can go straight to editing):
- `apps/web/src/features/ai-generate/director/context-relevance.ts` — `mapContextFlags` currently hardcodes `defaultAccept: false` for every flag. Change: split on confidence. `ContextFlag` ALREADY carries `confidence` (see `packages/hf-bridge/src/llm-context.ts:23`). Flag with `confidence >= DEFAULT_REDUNDANCY_ACCEPT_THRESHOLD` (0.7, exported from `redundancy-apply.ts:27`) -> `defaultAccept: true`; below -> `false`; missing/NaN confidence -> `false` (never auto-accept).
- The context prompt lives in `packages/hf-bridge/src/llm-context.ts` (grep for the prompt string / `throughline`). Harden it to name four target categories with one-line defs: (a) off-topic tangent, (b) abandoned/trailing-off thought, (c) meta-aside / self-correction ("wait that's wrong", "let me redo that", "scratch that"), (d) content belonging to a different video. Keep precision-over-recall; ask for a 0..1 confidence per flag; instruct that clear self-corrections/mistakes are usually high-confidence (safe to auto-remove) while topic-relevance judgments stay lower/opt-in.
- Tests: extend `apps/web/src/features/ai-generate/director/__tests__/context-relevance.test.ts` (0.85 -> defaultAccept true; 0.6 -> false; missing confidence -> false; overlap still filtered). The existing test at line 28 asserts `confidence` is 0.7 and `defaultAccept` false with the DEFAULT flag confidence of 0.7 — note the helper defaults confidence to 0.7, so update those assertions when 0.7 now auto-accepts (either bump the default in the helper or adjust the assertion), otherwise that test will break.
- Commit as `feat(director): auto-accept high-confidence out-of-context cuts + target meta-asides (2P-U4)`.

### 2P-U5 (no unnecessary cuts) — NOT started
Plan: `docs/plans/2026-07-03-001-...` unit U5 + R9. This is Dan's "you added a cut mid-continuous-speech with no mistake/silence" report (screenshots). Two facets:
1. Ensure `ConsolidateAdjacentClipsCommand` (`apps/web/src/commands/timeline/track/consolidate-adjacent-clips.ts`) merges EVERY source-contiguous adjacent same-source pair (pure split -> gone). Add a regression test.
2. New `apps/web/src/features/ai-generate/director/justify-cuts.ts` (pure): drop a sub-floor removal that sits between two complete content words and carries NO real reason (no filler/repeat/silence/dead-air/mistake). Reuse the content-word helper created in 2P-U1 (`apps/web/src/features/ai-generate/director/content-word.ts`). Fail-open with no transcript. Wire into `run-director.ts` (or apply-plan) before review. Tests per the plan's U5 scenarios.
- Diagnose FIRST on a real recut whether Dan's example is a pure split (consolidation gap) or a spurious tiny removal (detector false-positive) so the fix targets the right cause.
- Commit as `feat(director): drop unjustified cuts + guarantee pure-split consolidation (2P-U5)`.

### Live verification — NONE done
No recut-quality change (round 1 OR round 2) has been run through AI CUT on Dan's actual project. This is the real proof and it is entirely outstanding. Dan's 32-min "how to build a website" project is `d9b0924b-93af-4b1a-a10c-52e611327948`. When ready, run AI CUT (fresh recut, ideally on a throwaway copy) and confirm: no sub-10-frame slivers, no mid-continuous-speech cuts, residual repeats ("it's free / it's free to try" class) gone. Dev server: `bun run dev:web` from repo root -> localhost:3000.

## Working notes / gotchas

- Untracked docs are sitting in the repo (`docs/plans/*`, `docs/brainstorms/*`, `docs/ideation/*`, `docs/SESSION-HANDOFF-2026-07-02.md`). They were deliberately KEPT OUT of every feature commit. Do NOT `git add -A` — it sweeps these in. Always stage feature files by explicit path. **`docs/SESSION-HANDOFF-2026-07-02.md` must not be committed** (Dan's earlier rule).
- Standing rules: no em dashes anywhere (code/comments/docs), ponytail-minimal code, push only when Dan explicitly asks, present plans/docs as clickable links.
- AI Director model: in API-key mode the transcript reasoning (plan + redundancy + context passes) runs on `claude-opus-4-8`; in claude-code mode it inherits whatever the Claude CLI is set to; custom mode uses the user's model. Dispatch is `planDispatch` in `packages/hf-bridge/src/author.ts`.
- The tmux worker pattern (session `aicut` running `claude`) was used for most implementation with a Monitor watching approvals; it died twice mid-session and needed recreating. Doing the last two units inline (no worker) is faster now that browser automation is off the table.
- MIN_SURVIVING_CLIP_FRAMES = 15 is the shared floor constant introduced in 2P-U1; reuse it in 2P-U5.

## Suggested next-session order

1. `git log`/`git status` to confirm the state above.
2. Implement 2P-U4 inline (small: confidence split + prompt + tests), commit.
3. Implement 2P-U5 inline (consolidation regression + justify-cuts + tests), commit.
4. Run `bun test` (director green) + `bunx tsc --noEmit`.
5. Hand Dan the app for a real AI CUT verification run (or run the dev server and let him click) — do NOT use browser automation.
6. Only push when Dan says so.
