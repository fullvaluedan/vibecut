# Session Handoff, 2026-07-24 (rounds 11-14, UI overhaul, text round, review cycle, multi-pass in flight)

> Source of truth for the next session. Read alongside docs/plans/2026-07-24-001 (round-14 plan,
> the ACTIVE work), docs/2026-07-11-director-eval-findings.md ADDENDA 10-12 + postscripts,
> docs/plans/2026-07-19-002 (roadmap, statuses current), docs/SMOKE-20MIN.md (Dan's pending pass).
> Prior context: docs/HANDOFF-2026-07-18.md.

## TL;DR of a very large span (e4446a72 -> 7a4f9654, all pushed, PR #57)

1. **Director quality rounds 11-13 shipped and measured**: same-segment de-dup demotion (AUTO
   essLost -33% suite-wide, A10), join-texture + final-read + run feedback (A11, split verdict),
   final-read recall 6/14 -> 11/14 at precision 11/11 (A12, VERIFY_PROMPT_VERSION 6). The
   swallow-pause overlap fix merged measured (+4 words, in-noise, postscript in the findings doc).
2. **The whole UI overhaul landed** (roadmap 2026-07-19-002, W1-W8 all MERGED): chunked export
   audio (the 21-min wall is gone), panel slimming behind surface-flags, dead code out, transcript
   export (txt/srt/csv) + word-seek + search, masks deep fix (the 3 red tests fixed, OpenCut MIT
   attribution), design-system unification (NumberField/SliderNumberPair/Section everywhere),
   solids, text round (saved styles, stroke/shadow, font favorites), Cancel = one-step rollback.
3. **A 15-finding xhigh code review ran over the session diff; all 15 fixed, verified, merged**
   (10 CONFIRMED, 2 PLAUSIBLE, 2 refuted-dropped in verification, 2 more found by the gap sweep).
   ReportFindings ledger closed with outcomes.
4. **Dev-boot saga RESOLVED, root cause committed**: bun's isolated linker (.npmrc) symlinked
   `next` into the .bun store; Turbopack cannot resolve through it. Fix = `node-linker=hoisted`
   (c6f53c60) + exact next pins root AND apps/web (46e793a8). turbopack.root pins DO NOT fix it
   (tested twice; PATCHES.md says so). Canonical typecheck is `bunx tsc --noEmit` FROM apps/web;
   direct node_modules/.bin/tsc uses the wrong compiler (root TS 6) and false-fails.
5. **HyperFrames maintenance (feature stays PARKED)**: pins 0.7.10 -> 0.7.68 (render-smoke 24/24),
   registry fetches pinned to the engine's release tag (no more version-skew), findings doc
   docs/2026-07-22-hyperframes-remotion-findings.md (Remotion is NOT in this repo). Rust toolchain
   INSTALLED (rustup 1.29, stable 1.97.1, wasm32 target, wasm-pack 0.13.1, gnu linker, no admin).
6. **Round 14 (ACTIVE, Dan's 2026-07-24 directives)**: multi-pass Director. U0 pause-on-timeline-
   click MERGED. U3 per-project run ledger feeding taste notes MERGED. U1 multi-pass core IN
   FLIGHT (see loose ends). U2 (P3 final-read v2: revert-harmful + fragmentation guard) NOT
   started, launches after U1. U4 measurement (ADDENDUM 13) + U5 docs pending.

## Dan's latest verdicts and pending decisions

- From his real 208-op applied run (21-min footage): one-shot Director under-cuts (missing cuts,
  surviving repeats, "randomly tiny cuts"); he prescribed the 3-pass architecture + learning.
  NOTE his workflow: he SELECT-ALLs and applies everything, so P3 verification, not row defaults,
  is the real safety net (plan doc records this).
- Pause-on-click merged; he has NOT confirmed the feel. OPEN CALL for Dan: bookmark-marker clicks
  and keyboard seeks still seek-without-pausing (deliberately left; flagged to him).
- docs/SMOKE-20MIN.md (15 checks) still entirely un-run by Dan. The critical one is #10: export
  his real 29-minute project (chunked audio is headless-proven only).
- PRs: #57 carries everything; #56 still open; both await Dan.

## LOOSE ENDS a new session MUST check first (fragile local state, machine-local)

1. **U1 multi-pass worktree** `D:\Claude\framecut\.claude\worktrees\agent-a8c1602eb64544466`
   (branch worktree-agent-a8c1602eb64544466): the U1 agent built the virtual-apply + P2 loop,
   captured a pre-change eval baseline, and PARKED awaiting its background after-eval. Its work
   was UNCOMMITTED at last report. A new session cannot resume that agent. Check the worktree:
   if a commit exists, review + measure + merge per the plan's U1 contract; if uncommitted work
   exists, salvage by committing in the worktree; if the eval never finished, re-run it there
   (cache-warm, background, staggered per the eval-ops memory).
2. **Rust wasm worktree** `D:\Claude\framecut\.claude\worktrees\agent-a98aec260f6827c03`: the
   toolchain-install agent last reported "parity build compiling" and NEVER completed.
   MASK_EXPANSION_OPACITY_RENDERED is still false in masks/types.ts. Check the worktree for
   progress; the mission was: parity-build the in-repo rust/ engine, swap it in reversibly,
   implement expansion/opacity in the wgsl, flip the flag (spec in the 2026-07-22 turn's agent
   brief; W5 section of docs/plans/2026-07-19-003 has the staging).
3. Stale worktrees/branches from these two agents should be cleaned only AFTER salvage.

## Process contract (unchanged, load-bearing)

Fable plans ONLY; Opus/Sonnet/Haiku implement in per-feature worktrees (Dan directive, in
auto-memory). Worktree preamble: `git reset --hard feat/director-eval`, `bun install`, copy
`apps/web/.content-collections` from the main checkout (plus .env.local, .eval-cache,
eval-fixtures if the eval/diag is needed). Edit tool only; no em dashes in added lines; PATCHES.md
row in the SAME commit for upstream-originated files; prompt wording changes bump that pass's
version constant (DIRECTOR v2, DIRECTOR_P2 if U1 added one, RETAKE, STRUCTURAL, VERIFY v6);
new DirectorOpCategory needs the five-piece checklist; never edit pipeline code while an eval or
diag runs; live-LLM evals run_in_background and staggered from heavy agent load (they get killed
otherwise); eval essLost claims need --runs 3; capture eval output with a file redirect, never
Select-Object; PATCHES.md conflicts on EVERY worktree merge (keep both sections, then
`grep -nE "^<<<<<<<|^=======$|^>>>>>>>"` before commit AND repo-wide before push, a marker
slipped through twice); `bun mock.module` is process-global across the suite (the ledger round
hit this; push-model injection was the fix). Gates at tip 7a4f9654: apps/web 1705 pass 0 fail,
hf-bridge 204 pass, `bunx tsc --noEmit` clean from apps/web, `bun scripts/diag-join-the-group.ts`
green, `bun scripts/diag-join-verdicts.ts` = recall 11/14 precision 11/11.

## HyperFrames/Remotion stance (memory-backed)

Generation layer PARKED; UI hidden behind surface-flags.ts; maintenance (pin bumps, registry-tag
fix) is sanctioned when Dan asks; never propose un-parking - the findings doc holds the
recommendation and its preconditions.

## Pickup prompt for a new session (copy-paste)

```
You are picking up VibeCut (Dan's AI video editor). Repo: D:\Claude\framecut (git, branch
feat/director-eval @ 7a4f9654, pushed, PR #57). Read docs/HANDOFF-2026-07-24.md FIRST (loose
ends section especially), then docs/plans/2026-07-24-001-feat-director-round14-multipass-plan.md
(the ACTIVE plan), then docs/2026-07-11-director-eval-findings.md ADDENDA 10-12.

CONTEXT: Rounds 11-13 + the full UI overhaul + text round + a 15/15-fixed code review are merged
and pushed. Round 14 (multi-pass Director per Dan's 3-pass directive) is HALF DONE: U0
pause-on-click and U3 run-ledger MERGED; U1 multi-pass core was IN FLIGHT in worktree
.claude/worktrees/agent-a8c1602eb64544466 with UNCOMMITTED work awaiting a background eval -
salvage it first. A second stalled worktree (agent-a98aec260f6827c03) holds the Rust wasm
expansion/opacity work (toolchain is installed; flag still false).

FIRST MOVE: inspect both loose-end worktrees and salvage/finish U1 per the plan's U1 contract
(before/after eval table gates its merge). Then launch U2 (P3 final-read v2: revert-harmful,
last recall sweep, fragmentation guard for Dan's "randomly tiny cuts") on Opus, then run U4
(--runs 3 measurement, ADDENDUM 13) and U5 (docs via Haiku). Dan's smoke pass
(docs/SMOKE-20MIN.md, export-the-29-min-project first) is still owed and only he can run it.

PROCESS: Fable plans ONLY; delegate builds to Opus/Sonnet/Haiku in per-feature worktrees (reset
to feat/director-eval, bun install, copy apps/web/.content-collections + .env.local +
.eval-cache + eval-fixtures for eval work). Gates = apps/web 1705+/0, hf-bridge 204+/0, bunx tsc
--noEmit FROM apps/web (never direct node_modules/.bin/tsc - wrong compiler), diag-join-the-group
green, diag-join-verdicts recall >= 11/14 precision 11/11, four-fixture eval --runs 3 for essLost
claims, output captured via file redirect. Live-LLM evals: run_in_background, staggered from
heavy agent load. HARD RULES: Edit tool only; no em dashes; PATCHES.md same-commit for upstream
files; prompt changes bump version constants (VERIFY is at 6); five-piece checklist for new
categories; never edit pipeline code during an eval; PATCHES merge conflicts every time (keep
both sections, marker-grep before commit AND push); mock.module is process-global. The dev
server needs node-linker=hoisted (committed) - never revert .npmrc to isolated; turbopack.root
does not fix boot failures. HyperFrames generation stays PARKED (maintenance only, on request).
Dan is a coding novice directing the project: plain language, copy-paste commands, no raw stack
traces.
```
