# Session Handoff, 2026-08-01 (rounds 15-18 + onboarding shipped, paused on usage)

Source of truth: docs/plans/2026-08-01-001-feat-capcut-parity-roadmap.md (the ACTIVE
roadmap, statuses current) + docs/TO-VERIFY.md (dated round sections + Dan-owed list).
Branch feat/director-eval, everything through f00edced pushed. Dan paused the session
("continue when our usage resets").

## What shipped today (all G6-rated 9/10 unless noted)

- Round 15 (timeline CapCut feel): drop-to-V1 fix, magnetic main track (default ON),
  edge-clamp feedback, 8 video + 8 audio tracks with add/delete. CLOSED 9/9.
- Round 16 (transcript x Director): lineage journal, RED PIPES + per-word restore,
  panel UX, Groq error surfacing + local fallback, export-segment fix. CLOSED 9/9
  after one fix cycle. Groq server-side key (GROQ_API_KEY in apps/web/.env.local,
  real key present) verified end to end.
- Round 17 (prompt-to-edit assistant): context serializer + 11-tool schema + ask_user,
  executor (one BatchCommand per turn, strict >3 ops / >10s confirm), chat dock UI +
  mini-prompt, real-service adapter, template smart defaults + show-me mode.
  CLOSED 9/9. LIVE LLM RUN STILL DAN-OWED (route needs an Anthropic key: device key
  in Settings > AI, or ANTHROPIC_API_KEY in apps/web/.env.local + restart; claude-code
  mode support is T21.2).
- Round 18 (parity quick wins): speed curves (7 CapCut presets + graph), audio fade
  handles, export resolution/bitrate/SRT-alongside (lineage-aware), VibeCut home +
  wordmark + favicon. All 9/9. T18.1 (freeze/reverse/crop) was reopened for a
  freeze-frame linked-audio desync, FIXED (3e81c839, failing-test-first) and merged;
  the T18.1 RE-RATE AGENT WAS STILL RUNNING at pause time - check its result first;
  if it reported before you read this, its doc commit may need pushing.
- T21.1 onboarding (/get-started + first-run banner + provider cards + ai-settings
  deep link): CLOSED 9/9.

## Open items, in order

1. T18.1 re-rate result (see above) - push its doc commit if unpushed.
2. Dan's decisions: crop keyframes (defer recommended) vs build; then his feel checks
   (docs/TO-VERIFY.md Dan-owed list: exported-file playback, freeze/reverse/crop feel,
   real AI CUT to see Director reasons in the pipes, live assistant run with a key).
3. Round 19 (needs its own Fable plan docs per the roadmap): transitions, color
   adjust + filters, chroma key, effects registry + sounds unhide + stickers decision.
4. Round 20 (HyperFrames + Remotion, un-parked, probe-render-first): own plan doc.
5. Round 21 remainder: T21.2 provider abstraction (incl. claude-code mode for the
   assistant route), T21.3-T21.7 hosted credits/billing/shop (own plan doc; pricing
   recommendation already in roadmap section 10).
6. Two Dan-started background chips run in separate sessions (addTrack batching fix,
   diag cache receipt) - check their state before re-doing that work.

## Process notes added this session (also in auto-memory)

- Gates at tip f00edced: apps/web 2452 pass 0 fail (was 1777 at session start),
  hf-bridge 210, tsc clean from apps/web, build:web clean.
- Never pipe bun test through tail in the same chain as a push (a red tip got pushed
  once; caught and fixed same hour).
- Worktree agents MUST verify their base by test count before starting (two agents
  built on stale main; both salvaged at merge).
- diag-join-verdicts re-baselined: recall 12/16, precision 12/13, 19 fragments on the
  shared cache; the old 11/14 prose number is not comparable (cache drift, proven).
- PATCHES.md conflicts on every worktree merge: keep both sections, marker-grep
  before commit AND push.
