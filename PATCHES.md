# PATCHES.md — modified upstream files

Every change to a file that originally came from `OpenCut-app/opencut-classic` is logged here, in the same commit that makes the change (Hard rule 1 in `docs/BRIEF.md`).

**Upstream status:** `opencut-classic` was archived (read-only) on GitHub on 2026-05-17. No upstream merges will ever arrive, so these entries carry no live merge risk. The log is kept anyway: it is the map of what would need re-checking if FrameCut features are ever ported onto the OpenCut rewrite (`OpenCut-app/OpenCut`).

| File | Reason | Date | Notes for a future port |
|---|---|---|---|
| `apps/web/src/actions/keybinding.ts` | Archived main didn't build: added missing `isShortcutKey` guard + `MODIFIERS` set (incomplete upstream refactor) | 2026-06-11 | Rewrite likely restructures actions entirely; re-derive, don't copy |
| `apps/web/src/actions/definitions.ts` | Added missing `isAction` / `isActionWithOptionalArgs` guards (same incomplete refactor) | 2026-06-11 | Same as above |
| `apps/web/src/services/storage/migrations/runner.ts` | Fixed `IndexedDBAdapter` constructor calls to object-param form `{dbName, storeName, version}` | 2026-06-11 | Storage layer may be Rust-side in rewrite |
| `apps/web/src/services/storage/migrations/v1-to-v2.ts` | Fixed `.set()` calls to object-param form `{key, value}` | 2026-06-11 | Same as above |
| `apps/web/src/stickers/providers/index.ts` | Fixed `stickersRegistry.register({key, definition})` call shape | 2026-06-11 | — |
| `apps/web/tsconfig.json` | Excluded test files from build typecheck (upstream tests didn't compile) | 2026-06-11 | Revisit if we start running their tests |
| `.dockerignore` | Added nested `**/node_modules`, `**/.next` so local build artifacts don't contaminate the Docker image | 2026-06-11 | Safe to keep anywhere |
| `docker-compose.yml` | Fixed `web` healthcheck: `curl` doesn't exist in `oven/bun:alpine` and `localhost` resolved to IPv6; now `wget` against `http://127.0.0.1:3000/api/health` | 2026-06-11 | Cosmetic — app worked, `docker ps` just showed unhealthy |

All eight patches were authored and build-validated (`bun run build:web` exit 0) during the 2026-06-07 local bring-up of opencut-classic, and committed to this fork on 2026-06-11.

## Feature patches (FrameCut functionality on upstream files)

| File | Reason | Date | Notes for a future port |
|---|---|---|---|
| `apps/web/src/timeline/timeline-store.ts` | Added `videoWaveformsEnabled` flag (default true) + toggle, persisted | 2026-06-11 | Plain zustand UI flag; trivial to re-add |
| `apps/web/src/timeline/components/timeline-element.tsx` | `TiledMediaContent` renders an `AudioWaveform` strip on the bottom 40% of video clips with source audio (reuses audio-clip waveform infra + cache) | 2026-06-11 | Core feature; reimplement against whatever clip renderer the rewrite ships |
| `apps/web/src/timeline/components/timeline-toolbar.tsx` | Toolbar toggle button for video-clip waveforms (next to snapping/ripple); RUN HYPERFRAMES button; Close Gaps button | 2026-06-11 | Trivial |
| `apps/web/src/actions/use-editor-actions.ts` | Delete with empty selection now closes the main-track gap under the playhead (CloseGapsCommand at-time) | 2026-06-11 | Generic timeline UX — candidate for upstreaming to the rewrite |
| `apps/web/src/components/editor/panels/properties/registry.tsx` | Registered "HyperFrames" properties tab (template swap/content edit) for AI-generated video elements; default tab when `framecutAi` present | 2026-06-11 | Tab content lives in `features/ai-generate/` |
| `apps/web/src/components/editor/panels/assets/views/assets.tsx` | Bin upgrades: Delete-key removes selected assets; Assemble button; "Convert for editing" context item (local ffmpeg transcode); "No preview" badge for undecodable videos; plus-button insert now routes via `features/editing/insert-media` (auto audio separation) | 2026-06-11 | Several small hooks; feature logic lives in `features/editing/` |
| `apps/web/src/media/processing.ts`, `services/storage/{types.ts,service.ts}` | Persist `canDecode`/`codec` on media assets; FIX upstream bug: `hasAudio`/`fps` were never saved in metadata (lost on reload); request `navigator.storage.persist()` so OPFS media can't be evicted | 2026-06-11 | The hasAudio/fps fix is upstream-worthy |
| `apps/web/src/core/managers/renderer-manager.ts` | `exportProject` accepts optional `sceneTracks` (renders a non-active scene — powers nested sequences) | 2026-06-11 | Small parametrization, upstream-worthy |
| `apps/web/src/components/editor/scenes-view.tsx` | "Nest" button per scene row (renders scene → clip at playhead via `features/editing/nest-scene`); "Add scene" button | 2026-06-11 | UI hook only |
| `apps/web/src/services/renderer/scene-builder.ts` | Exclude `framecutAi` video elements from the render tree (WebCodecs can't decode their VP9 alpha → they composited as opaque black). Preview shows them via DOM `<video>` layer; exports burn them in with local ffmpeg | 2026-06-11 | CRITICAL: any future base must handle alpha overlays |
| `apps/web/src/preview/components/index.tsx` | Mounted `AiOverlayPreviewLayer` (DOM video layer, playhead-synced) over the preview canvas | 2026-06-11 | One sibling div |
| `apps/web/src/components/editor/export-button.tsx` | Export now composites AI overlays via `/api/media/composite` before download (forces mp4 when burned) | 2026-06-11 | Wiring only; logic in `features/ai-generate/composite-export.ts` |
| `apps/web/src/actions/{definitions.ts,keybinding.ts}` | New actions `timeline-zoom-in`/`-out` with default keys `=`/`+`/`-` (added `- = +` to the key set); handled in timeline-toolbar | 2026-06-11 | Upstream-worthy |
| `apps/web/src/actions/{definitions.ts,keybinding.ts}` + timeline-toolbar | New action `timeline-zoom-fit` bound to `\` (zoom out to fit the whole timeline = minZoom) | 2026-06-11 | Upstream-worthy |
| `apps/web/src/timeline/{zoom-utils.ts,components/index.tsx,components/timeline-toolbar.tsx}` | `\` fit now uses new `getTimelineZoomFit` (content fills 95% of viewport) instead of minZoom (which keeps 75% padding) | 2026-06-12 | Small util + prop |
| `apps/web/src/timeline/{controllers/drag-drop-controller.ts,hooks/use-timeline-drag-drop.ts}` | Drag-drop video inserts now separate source audio onto an audio track (insertAtTarget returns ids; new optional `separateSourceAudio` config hook) | 2026-06-12 | Mirrors features/editing/insert-media behavior |
| `apps/web/src/timeline/components/timeline-element.tsx` | Clip context menu gains "Nest selection..." and "Remove attributes" submenu (Motion/Opacity/Audio/All keyframes/Everything → `features/editing/remove-attributes.ts`) | 2026-06-12 | Two menu blocks |
| `apps/web/src/preview/components/{index.tsx,toolbar.tsx}` | Text/Shape place tools: T button in preview toolbar + click-capture overlay (`place-tool-overlay.tsx`, ours) that drops text/shapes at the clicked canvas position | 2026-06-12 | One mount + one button |
| `apps/web/src/components/editor/panels/assets/{assets-panel-store.tsx,index.tsx}` | Stickers tab replaced by Shapes tab (graphics rectangle/ellipse/polygon/star; view in `graphics/components/assets-view.tsx`, ours). Stickers code remains, just unrouted | 2026-06-12 | Tab swap only — revert = swap back |
| `apps/web/src/effects/components/assets-view.tsx` | (2026-06-12 PM: reverted — browser moved to the HyperFrames tab; Effects shows effects only) | 2026-06-12 | — |
| `apps/web/src/components/editor/panels/assets/{index.tsx,assets-panel-store.tsx,views/base-panel.tsx}` | Panel maximize: ` action + double-click panel header → assets panel fills the screen (fixed inset-2 z-50), Esc restores | 2026-06-12 | Premiere parity |
| `apps/web/src/actions/{definitions.ts,keybinding.ts}` | New action `toggle-panel-maximize` bound to `` ` `` (added to key set) | 2026-06-12 | Upstream-worthy |
| `apps/web/src/components/editor/panels/properties/components/property-param-field.tsx` | Number params without a shortLabel now get a default "↔" scrub handle — font size & every panel number becomes click-drag adjustable | 2026-06-12 | One-line default |
| `apps/web/src/preview/components/toolbar.tsx` + `graphics/definitions/index.ts` | Pen/shape tool button next to the Text tool (click = pen, hold = shape flyout); registers new `custom-path` graphic definition (`definitions/path.ts`, ours: pen-drawn polygon with Feather + Expand) | 2026-06-12 | One button + one registration |
| `apps/web/src/timeline/components/index.tsx` | Premiere chrome: `TimelineToolRail` (tool-rail.tsx, ours) mounted left of track labels; V1/V2/A1/T1/G1 track badges in TrackLabelsPanel | 2026-06-12 | Rail is a sibling mount; badges one span |
| `apps/web/src/actions/definitions.ts` | Default binding `m` for existing `toggle-bookmark` (Premiere marker key) | 2026-06-12 | One line |
| `apps/web/src/core/index.ts` + `timeline/types.ts` + `commands/timeline/track/add-track.ts` | FOUND the empty-track pruning (a command reactor in core). New `keepWhenEmpty` track flag respected by the reactor; AddTrackCommand can set it — user-added tracks persist like Premiere's | 2026-06-12 | Root-cause fix for "Add track" |
| `apps/web/src/app/editor/[project_id]/page.tsx` + assets panel/base-panel | ` now maximizes the HOVERED panel (assets/preview/properties/timeline) via shared `editor/panel-maximize-store`; MaximizablePanel wrapper in the layout | 2026-06-12 | Premiere parity |
| `apps/web/src/timeline/components/index.tsx` | Track context menu: Add video/audio track (keepWhenEmpty), Move track up/down (`commands/timeline/track/move-track.ts`, ours) | 2026-06-12 | Menu block |
| `apps/web/src/subtitles/components/assets-view.tsx` | Mounts `CaptionStyles` (caption-styles.tsx, ours): HyperFrames-inspired one-click looks for generated captions | 2026-06-12 | One mount |
| `apps/web/src/app/editor/[project_id]/page.tsx` + panel-maximize-store | Active-panel model: clicking a panel selects it (focus ring), ` toggles the SELECTED panel full screen | 2026-06-12 | Premiere active-panel |
| `apps/web/src/timeline/components/timeline-toolbar.tsx` | `\` is now a toggle: fit ↔ the zoom you were at before fitting | 2026-06-12 | Small ref |
| `apps/web/src/actions/{definitions.ts,use-editor-actions.ts}` | New `go-to-previous-edit`/`go-to-next-edit` actions on Up/Down — playhead jumps between clip boundaries (Premiere edit points) | 2026-06-12 | Upstream-worthy |
| `apps/web/src/timeline/hooks/use-edge-auto-scroll.ts` | Guard against mouseX=0 during drag-state transitions (caused spurious hard-left auto-scroll bursts mid-drag) | 2026-06-12 | Bug fix |
| `apps/web/src/components/editor/panels/properties/components/property-param-field.tsx` | Premiere-style controls: selects with ≤4 options render as segmented buttons (textAlign etc.); font param renders a common-system-fonts dropdown; fontSize gets ▲/▼ steppers | 2026-06-11 | Pure control-rendering changes |
| `apps/web/src/components/editor/panels/properties/registry.tsx` | Transform tab now renders the Premiere-style `EffectControlsTab` (fx Motion/Opacity groups, paired Position, Uniform Scale, blue scrub values) instead of the flat param list | 2026-06-11 | New tab component is ours (`effect-controls-tab.tsx`); revert = point back to ElementParamsTab |
| `apps/web/src/commands/timeline/track/remove-ranges.ts` (new, ours) + timeline-toolbar hooks | RemoveRangesCommand: cut time ranges across all tracks (split straddlers, ripple left) — powers Remove Silences/Repeats/Autocut (`features/editing/`); toolbar gains AI CUT menu + Nest-selection button | 2026-06-11 | Command is upstream-worthy |
| `apps/web/src/timeline/types.ts` | Added optional `framecutAi` metadata to `VideoElement` (compId/templateId/variables/groupId) — links AI clips to their comp source for re-render and template swap | 2026-06-11 | Core data-model addition; must be carried to any future base |
| `apps/web/src/app/layout.tsx` | React Scan dev overlay now opt-in via `NEXT_PUBLIC_REACT_SCAN=1` (was always-on in dev; confused non-dev use of the dev server) | 2026-06-11 | Trivial |
| `site/brand.ts`, `app/metadata.ts`, `components/header.tsx`, `components/landing/hero.tsx`, `components/editor/{mobile-gate,onboarding}.tsx`, `services/storage/components/storage-persistence-dialog.tsx`, `auth/server.ts` | VibeCut rebrand: visible "OpenCut" strings → "VibeCut" (upstream-history pages like changelog/contributors left untouched) | 2026-06-11 | String-only |
| `apps/web/src/components/editor/panels/assets/views/settings/index.tsx` | Registered "AI" tab in the settings panel; content lives in `features/ai-generate/` | 2026-06-11 | 4-line hook-in; re-register against whatever settings UI the rewrite ships |
| `apps/web/src/components/editor/panels/assets/{assets-panel-store.tsx,index.tsx}` | Registered "HyperFrames" left-panel tab (template browser with demo previews + checkboxes that constrain the planner); content in `features/ai-generate/components/hyperframes-panel.tsx` | 2026-06-11 | Two small hook-ins |
| `apps/web/next.config.ts` | Added `transpilePackages: ["@framecut/hf-bridge"]` + `serverExternalPackages: ["hyperframes"]` (CLI is resolved at runtime, must not be bundled) | 2026-06-11 | Two lines |
| `apps/web/package.json` | Added `@framecut/hf-bridge: workspace:*` dependency | 2026-06-11 | One line |

| `apps/web/src/actions/definitions.ts` | Premiere hotkeys round 11: new actions `ripple-delete`/`select-clip-at-playhead`/`track-select-forward`/`open-speed-panel`; default bindings Ctrl+K=split, Shift+Delete/Backspace=ripple delete, D, A, Ctrl+L=link/unlink AV, Ctrl+R=speed panel | 2026-06-12 | All in ACTION_DEFAULT_SHORTCUTS + action list |
| `apps/web/src/actions/keybindings-store.ts` | Persisted-keymap merge is now ADDITIVE: defaults whose key is still free are adopted on load, so new shortcuts reach existing users without a keymap reset | 2026-06-12 | Small merge() change; upstream-worthy |
| `apps/web/src/actions/use-editor-actions.ts` | Q/W are true Premiere ripple trims (previous/next edit point to playhead via RemoveRangesCommand, cross-track); handlers for ripple-delete (selection spans removed on ALL tracks), D select-at-playhead (first frame inclusive), A track-select-forward, Ctrl+R opens Speed tab | 2026-06-12 | Replaces old split-left/right handlers |
| `apps/web/src/export/index.ts` | New `saveBufferWithPicker`: File System Access save dialog with `id: "vibecut-export"` so Chrome re-defaults to the last export folder; AbortError = cancelled, any other failure falls back to plain download | 2026-06-12 | Pure addition |
| `apps/web/src/components/editor/export-button.tsx` | Export uses saveBufferWithPicker (choose destination, remembered) with saved/cancelled toasts; the three export sections (Format/Quality/Audio) now default open | 2026-06-12 | Swap call + defaultOpen flips |
| `apps/web/src/components/editor/panels/properties/index.tsx` | Typo fix: multi-select placeholder rendered a stray "0" after "elements selected." | 2026-06-12 | One char |
| `apps/web/src/timeline/components/timeline-track.tsx` | Premiere gap selection (click between clips → highlighted gap on that track) + Track Select Forward tool clicks (all-tracks / Shift = one track) + gap highlight render | 2026-06-12 | Background mouseUp hook + one overlay div |
| `apps/web/src/actions/use-editor-actions.ts` | Delete/Shift+Delete ripple-delete a SELECTED gap across all tracks, blocked (toast) when another track's clip overlaps the span — Premiere rule; A now arms the Track Select Forward tool; Escape clears gap/tool; self-learning hooks (undo attribution, AI-effect deletion tracking) | 2026-06-12 | Gap store + preference store are ours |
| `apps/web/src/actions/definitions.ts` | track-select-forward description updated to the tool behavior | 2026-06-12 | One string |
| `apps/web/src/components/editor/panels/assets/views/settings/index.tsx` | Settings gains Hotkeys tab (embedded ShortcutsEditor + reset) and Help tab (help.tsx, ours) | 2026-06-12 | Two tabs + content blocks |
| `apps/web/src/actions/components/shortcuts-dialog.tsx` | Recording UI extracted into exported `ShortcutsEditor` (dialog now wraps it); unbound actions get a "Not set" record button | 2026-06-12 | Pure refactor + one affordance |
| `apps/web/src/preview/components/toolbar.tsx` | Preview Text/Pen buttons removed (tools live in the timeline rail) and replaced by the AI prompt box (`assistant-prompt.tsx`, ours); place-tool Esc handling kept via `usePlaceToolEscape` | 2026-06-12 | Toolbar right side reshaped |
| `apps/web/src/sounds/components/assets-view.tsx` | Sounds panel gains "Music & SFX" tab (HeyGen semantic search; view in `heygen-sounds.tsx`, ours) | 2026-06-12 | Tab + content registration |
| `apps/web/src/components/editor/export-button.tsx` | Export records the AI-Cut-to-export diff into the self-learning store (noteExport) | 2026-06-12 | Three lines |
| `apps/web/src/features/ai-generate/store.ts` | Device-local heygenApiKey + serpApiKey fields (same pattern as anthropicApiKey) | 2026-06-12 | Settings → AI → Integrations |
| `apps/web/src/components/ui/number-field.tsx` + `properties/components/effect-controls-tab.tsx` | Scrub drags use pointer CAPTURE instead of pointer lock — Chromium left the cursor invisible after exitPointerLock until the next click | 2026-06-12 | Root cause of "mouse disappears after dragging a value" |
| `apps/web/src/preview/components/place-tool-overlay.tsx` | Pen matches Premiere: paths close ONLY by clicking the first vertex (no double-click close), open polyline + big white first-vertex handle while drawing; mask failures with a clip selected now error instead of silently creating a shape layer | 2026-06-12 | Behavior + feedback |
| `apps/web/src/graphics/components/graphic-tab.tsx` | "Points (pen data)" hidden from the custom-shape panel (internal geometry; redraw to change) | 2026-06-12 | One filter |
| `apps/web/src/app/editor/[project_id]/page.tsx` | Active panel outline is now a bright ring-2 + outer glow; mounts BackgroundTranscriber (features/transcription/, ours) | 2026-06-12 | Glow + one mount |
| `apps/web/src/features/editing/remove-repeats.ts` + `ai-generate/run-hyperframes.ts` | Both consume the shared transcript cache (ensureTimelineTranscript) — instant when the background transcriber already covered this timeline state; live elapsed ticker during model init | 2026-06-12 | Pipeline centralized in transcript-cache.ts (ours) |
| `apps/web/src/features/ai-generate/store.ts` | backgroundTranscriptionEnabled flag (default ON) | 2026-06-12 | Settings → AI toggle |
| `apps/web/src/core/managers/playback-manager.ts` | Playback rate (0.25-4): setPlaybackRate/getPlaybackRate; the rAF clock scales elapsed wall time; rate changes rebase via pause/play | 2026-06-12 | Core playback addition |
| `apps/web/src/core/managers/audio-manager.ts` | Audio follows preview speed: sessionRate captured per play session; timeline<->context time mapping scaled (toContextTime), buffer sources get combined retime x session rate, late-start offsets converted to source seconds | 2026-06-12 | Scheduling math change |
| `apps/web/src/preview/components/toolbar.tsx` | Playback speed slider 0.5x-3x next to the play button (double-click label = back to 1x; preview-only) | 2026-06-12 | One component |
| `apps/web/src/timeline/types.ts` | TextElement gains optional `motionTemplate {templateId, groupId, variables}` — native-template marker (resize re-bake, AI recognition, future swap) | 2026-06-12 | Mirrors framecutAi on VideoElement |
| `apps/web/src/timeline/element-utils.ts` | buildTextElement passes raw.animations + raw.motionTemplate through (previously dropped) | 2026-06-12 | Two spread lines |
| `apps/web/src/timeline/update-pipeline.ts` | Duration changes on motionTemplate text elements re-bake animations via retimeTemplateAnimations (entrances pinned to start, exits follow the new end) before the clamp rule | 2026-06-12 | New enforce rule; retime fn in animation/template-retime.ts (ours) |
| `apps/web/src/text/components/assets-view.tsx` | Text tab mounts the Motion templates gallery (features/motion-templates/, ours) | 2026-06-12 | One mount |
| `apps/web/src/graphics/definitions/index.ts` | Registers `swiss-grid` definition (swiss-grid.ts: full-frame plate with transparent video window, all params keyframable) | 2026-06-12 | One registration |
| `apps/web/src/features/ai-generate/{store.ts,components/hyperframes-panel.tsx,run-hyperframes.ts}` | hfEngine "native" (default) / "cinematic" segmented control; native branch places motion-template elements directly after planning (no CLI render, no media import, instant) | 2026-06-12 | Cinematic path unchanged |
| `apps/web/src/app/api/media/composite/route.ts` | ffmpeg burn-in probes hardware H.264 once (nvenc → qsv → amf) and uses it with per-request libx264 fallback | 2026-06-12 | 3-8x faster cinematic exports on GPUs |
| `apps/web/src/components/editor/panels/properties/registry.tsx` | Text elements with `motionTemplate` get a "Template" tab (default) — TemplateControlsTab edits the template's fields + timing; Audio tab gains the A/V sync readout + Realign (AudioSyncSection, ours) | 2026-06-13 | Conditional tab + one section mount |
| `apps/web/src/timeline/types.ts` | `TextElement.motionTemplate` already added (R16); `BaseTimelineElement.linkId?` added — shared id linking a video and its separated audio | 2026-06-13 | One optional field |
| `apps/web/src/timeline/element-utils.ts` | buildTextElement passes `hidden` through (stable-count templates: section-break kicker hidden when empty) | 2026-06-13 | One spread line |
| `apps/web/src/timeline/update-pipeline.ts` | Template re-bake-on-resize rule skips when the patch already supplies `animations` (Template Controls writes coherent end-pinned keyframes itself) | 2026-06-13 | One guard |
| `apps/web/src/commands/timeline/element/toggle-source-audio-separation.ts` | Stamps a shared `linkId` on the video + its separated audio (linked clips) | 2026-06-13 | linkId reused/generated |
| `apps/web/src/timeline/controllers/element-interaction-controller.ts` + `hooks/element/use-element-interaction.ts` | Linked selection: clicking/drag-selecting one clip unions its linked partners (Alt = solo); `selectMany` added to the selection API | 2026-06-13 | Reuses existing multi-select move/trim/delete |
| `apps/web/src/timeline/timeline-store.ts` | `linkedSelectionEnabled` flag (default on, persisted, migrate v1) | 2026-06-13 | Toolbar chain toggle |
| `apps/web/src/timeline/components/timeline-toolbar.tsx` | Chain-link toggle button for linked selection | 2026-06-13 | One ToolbarButton |
| `apps/web/src/timeline/components/timeline-element.tsx` | Amber `⚠ Nf` A/V-sync badge on clips whose audio/video partner has drifted (computeAvSyncOffset, ours) | 2026-06-13 | One overlay component |
| `apps/web/src/core/index.ts` | Dev-only `window.__vibeEditor` for console inspection (stripped in production builds) | 2026-06-13 | Gated on NODE_ENV |
| `apps/web/src/components/editor/panels/properties/stores/properties-store.ts` | Uniform Scale defaults ON (Premiere Motion default) — uncheck to scale H/W independently | 2026-06-13 | One default flip |
| `apps/web/src/timeline/element-utils.ts` | buildTextElement passes `linkId` through (template groups + linked A/V share the field) | 2026-06-13 | One spread line |
| `apps/web/src/timeline/types.ts` | Bake library: added optional `framecutAi.registryBlock` (holds the baked registry block name; marks the clip as a baked BLOCK so the properties panel offers re-bake instead of a template swap) | 2026-06-13 | One optional field; all bake logic lives in `features/ai-generate/` + `packages/hf-bridge/bake.ts` |
| `apps/web/src/components/editor/panels/properties/index.tsx` | Group-aware properties panel: when a multi-selection is exactly ONE motion-template group (all text elements sharing a registered template's `motionTemplate.groupId`), render that template's Template Controls instead of the "N elements selected" placeholder — so a linked-selected template group is editable. Mixed selections still show the placeholder. | 2026-06-13 | `singleTemplateGroupRepresentative` helper + one branch before the placeholder; logic reusable |
| `apps/web/src/services/renderer/scene-exporter.ts` | Export speed: set `hardwareAcceleration: "prefer-hardware"` on the export `CanvasSource` (WebCodecs video encoder) so it uses the GPU's hardware encoder (NVENC/QSV/AMF/VideoToolbox) when available; falls back to software automatically | 2026-06-13 | One config key; the renderer's biggest export-speed lever |
| `apps/web/src/timeline/types.ts` | Template scale: added optional `motionTemplate.scale` (user size multiplier persisted on the element so resizing round-trips) | 2026-06-13 | One optional field; threaded via `features/motion-templates/` |
| `apps/web/src/timeline/controllers/playhead-controller.ts` | Editor-UX fix: the playhead-follow auto-scroll in `handlePlaybackUpdate` was gated to playback only; now it also follows on seeks (keyboard nudges/jumps) — guard suppresses scroll only while actively scrubbing | 2026-06-17 | One guard line |
| `apps/web/src/timeline/controllers/seek-controller.ts` | Editor-UX fix: seek-on-click scoped to the ruler band only — a plain click in the track area deselects but no longer moves the playhead (`handleClick` returns before `seekFromEvent` when `source === "tracks"`) | 2026-06-17 | One branch in handleClick |
| `apps/web/src/timeline/timeline-store.ts` | Editor-UX: added persisted `timelineNudgeFrames` (default 15) + `setTimelineNudgeFrames` for the Shift+←/→ nudge amount; partialize + version 2 migrate | 2026-06-17 | Plain zustand UI pref, mirrors videoWaveformsEnabled/linkedSelectionEnabled |
| `apps/web/src/actions/use-keybindings.ts` | Editor-UX fix: stop the dispatcher forcing `{seconds:5}` on jump-forward/backward (the real source of the 5s jump); they now fall through with no args so the handler reads the configurable frame nudge | 2026-06-17 | Remove two switch arms |
| `apps/web/src/actions/use-editor-actions.ts` | Editor-UX fix: jump-forward/backward now nudge by `timelineNudgeFrames` (default 15) converted via project fps (new `jumpDelta` helper); `args.seconds` kept as a programmatic escape hatch | 2026-06-17 | Mirrors the frame-step recipe |
| `apps/web/src/actions/definitions.ts` | Editor-UX: jump-forward/backward descriptions updated from "5 seconds" to the configurable-frames behavior | 2026-06-17 | Two strings |
| `apps/web/src/components/editor/panels/assets/views/settings/index.tsx` | Editor-UX: Settings → Hotkeys gains a "Shift + ← / → nudge" frames NumberField bound to `timelineNudgeFrames` | 2026-06-17 | One Section in HotkeysContent |
| `apps/web/src/timeline/components/timeline-track.tsx` | Editor-UX: Track Select Forward selection is now MOVABLE — dragging a clip already in the forward selection moves the whole selection (open a gap, then drag a cut clip's head back into it); a click still selects-forward. `selectForwardFrom` → object params | 2026-06-17 | onElementMouseDown gate (drag only when selected) + onElementClick guard |
| `apps/web/src/timeline/components/timeline-track.tsx` | #5 follow-up: pressing an UNSELECTED clip with the forward tool now selects-forward AND starts the drag in one gesture (was two-step + undiscoverable, so it read as "can't move multiple"). | 2026-06-18 | selectForwardFrom on mousedown, fall through |
| `apps/web/src/timeline/hooks/element/use-element-interaction.ts` | #5 follow-up: the interaction controller's selection reads are now LIVE (editor.selection.getSelectedElements()) instead of the React render snapshot, so a selection made earlier in the same synchronous gesture (forward-select on mousedown) is the move group. | 2026-06-18 | getSelected/isSelected live |
| `apps/web/src/components/editor/export-button.tsx` | Editor-UX fix: a running export no longer self-cancels when the popover is dismissed (outside-click/Escape) — `handlePopoverOpenChange` keeps the popover open while `isExporting`; the explicit Cancel button is the only stop. (The 5%-stall root cause — audio stage decodes the whole timeline with no progress/cancel — is a separate follow-up.) | 2026-06-17 | One guard + one selector |
| `apps/web/src/core/managers/commands.ts` | Memory hardening: cap undo history at `MAX_HISTORY=200` (was unbounded) via `pushHistory()` — each entry can retain cloned timeline state, so a long session grew memory and likely contributed to the reported playback stutter (a reload cleared it). Per-entry payload size (TracksSnapshotCommand clone) + heap-profiling confirmation are a follow-up. | 2026-06-17 | pushHistory() trims oldest in execute/push/redo |
| `apps/web/src/transcription/types.ts` | Director duplicate-word feature (#1): added `TranscriptionWord {text,start,end}` + optional `words?` on `TranscriptionResult` (present only in word-timestamp mode) | 2026-06-18 | Two additive types |
| `apps/web/src/services/transcription/worker.ts` | Director #1: opt-in `wordTimestamps` mode on the transcribe message — uses `return_timestamps:"word"`, returns per-word timing AND rebuilds phrase segments from the words (`segmentsFromWords`) so segment consumers are unaffected. Default path unchanged. | 2026-06-18 | Additive branch in handleTranscribe |
| `apps/web/src/services/transcription/worker.ts` | Director #1 fix: repair chunk-boundary words — transformers.js returns a null end at each 30s seam; infer the end from the next word's start instead of collapsing it to start (a zero-length point the duplicate detector dropped + which corrupted gap math). | 2026-06-18 | Word-collection loop now indexed |
| `apps/web/src/services/transcription/service.ts` | Director #1: thread `wordTimestamps` through `transcribe()` + return `words` in the result | 2026-06-18 | Two additive lines |
| `apps/web/src/media/audio.ts` | Export #7: `createTimelineAudioBuffer`/`collectAudioElements` now take an optional `onProgress`/`onDecodeProgress` and emit 0..1 across decode→mix→master. Behavior-preserving (muted elements still skipped from the mix; only the progress counter advances). | 2026-06-18 | Additive callbacks; the audio stage no longer looks frozen at 5% |
| `apps/web/src/core/managers/renderer-manager.ts` | Export #7: feed `createTimelineAudioBuffer`'s progress into the first 5% of the export bar (0.01 + fraction·0.04) so the audio build shows real movement instead of a deceptive freeze. (Cancel-DURING the audio stage — moving the watchdog earlier — is a follow-up.) | 2026-06-18 | Pass onProgress to the audio build |
| `apps/web/src/timeline/placement/resolve.ts` | Import-to-V1 fix: `firstAvailable` now diverts a VIDEO element to the main (V1) track when it can hold the span, instead of grabbing the first overlay video track (orderedTracks lists overlay before main, so an empty V2 won otherwise). text/graphic/audio unchanged; matches Assemble's explicit-main behavior. | 2026-06-18 | One video short-circuit in firstAvailable |
| `apps/web/src/export/index.ts` | Export save-order: split `saveBufferWithPicker` into `pickSaveLocation` (show the save dialog up front) + `writeBufferToHandle` (write after encode); kept the old fused wrapper. Typed `window.showSaveFilePicker` via a global augmentation (removed an `as unknown as` cast). | 2026-06-18 | Pick/write split + SaveLocation type |
| `apps/web/src/components/editor/export-button.tsx` | Export save-order: `handleExport` now asks WHERE to save BEFORE encoding (cancel costs nothing); predicts the mp4-vs-selected container up front via the pure `collectAiOverlayClips` so the dialog shows the right extension; encodes → composites → writes to the picked handle (plain-download fallback). | 2026-06-18 | Pick-first reorder of handleExport |
| `apps/web/src/commands/timeline/element/unlink-elements.ts` (new, ours) + `element/index.ts` | #4B: `UnlinkElementsCommand` strips a shared `linkId` from a video + its separated-audio partner (keeps both clips) so they move/trim independently; undo restores the link via a tracks snapshot. Distinct from Extract/Recover audio (which keeps the link). | 2026-06-18 | Snapshot command; export added |
| `apps/web/src/core/managers/timeline-manager.ts` | #4B: `unlinkElements({linkId})` constructs + executes `UnlinkElementsCommand` (mirrors `toggleSourceAudioSeparation`). | 2026-06-18 | One method |
| `apps/web/src/actions/{definitions.ts,use-editor-actions.ts}` | #4B: new `unlink-elements` action (editing, no default key) + handler that unlinks the single selected element's link group. | 2026-06-18 | Mirrors toggle-source-audio |
| `apps/web/src/timeline/components/timeline-element.tsx` | #4B: "Unlink audio/video" context-menu item, shown only when the selected element has a `linkId`. | 2026-06-18 | One ContextMenuItem |
| `apps/web/src/timeline/group-move/resolve-move.ts` | #4A: cross-track move for linked A/V — `resolveExistingTrackIdsByElementId` no longer aborts (return null) when a non-anchor member finds no compatible track; it keeps that member on its CURRENT track so the anchor (video) moves between tracks while the linked audio partner stays put (Alt no longer required). Genuine overlaps still rejected by `canApplyMovesToExistingTracks`. | 2026-06-18 | Two `return null` → keep-on-current-track + continue |
| `apps/web/src/preview/components/index.tsx` | U1 preview-freeze fix: the render-loop promise now `.catch(log).finally(() => renderingRef = false)` instead of resetting the in-flight guard only in `.then` — a rejected render (e.g. undecodable HEVC frame) no longer leaves `renderingRef` stuck true, which had bricked the preview for the whole session. | 2026-06-18 | One promise-chain change in the render useCallback |
| `apps/web/src/services/renderer/resolve.ts` | U1 defense-in-depth: `resolveVideoNode`'s `videoCache.getFrameAt` is wrapped in try/catch → `return null`, so one undecodable clip renders as empty instead of rejecting the whole render-tree resolve. | 2026-06-18 | One try/catch around getFrameAt |
| `apps/web/src/services/video-cache/service.ts` | U1 negative cache: added `undecodableMediaIds` Set — when `initializeSink` throws (unsupported codec), `ensureSink` records the mediaId and swallows the error; subsequent `getFrameAt`/`ensureSink` short-circuit to `null` instead of re-creating the mediabunny `Input` and re-throwing every frame. `clearVideo` clears the entry so re-import/replace can retry. | 2026-06-18 | New Set + early returns in getFrameAt/ensureSink + clear in clearVideo |
| `apps/web/src/components/editor/panels/assets/views/assets.tsx` | U1 bin warning: extracted the "No preview" badge into `NoPreviewBadge` and render it over the placeholder tile too — undecodable videos have no thumbnail (thumbnail render is gated on canDecode), so they previously fell through to a plain "Video" tile with no warning. | 2026-06-18 | Badge component + render in the no-thumbnail video branch |
| `apps/web/src/components/editor/panels/properties/index.tsx` | U2 state hygiene: keyed the tab content by `element.id` (`<div key={element.id} className="contents">`) so the subtree REMOUNTS on selection change and every per-element draft `useState` re-seeds (Template Controls vars/duration/scale, ScaleRows Uniform-Scale, FxGroup collapse) — fixes the HIGH-severity bug where switching template groups showed/saved the PREVIOUS group's values. Also added a compact "N elements selected — editing <name>" header for a true multi-select (not a single template group, not a linked V/A pair). | 2026-06-18 | `key` wrapper + `isTrueMultiSelect` header; layout wrapped in a flex column so the header sits above the tabs |
| `apps/web/src/components/editor/panels/properties/stores/properties-store.ts` | U2 dead-code: removed the unused `isTransformScaleLocked` field + `setTransformScaleLocked` setter (grep-confirmed zero references — ScaleRows uses local component state; the global lock was never wired). | 2026-06-18 | Store shrinks to `activeTabPerType` + `setActiveTab`; revert is additive if the global lock is ever needed |
| `apps/web/src/utils/browser.ts` | U3 shortcut guards: added `INTERACTIVE_DOM_SELECTOR` + `isInteractiveDOMElement(el)` (button / a[href] / select / [role=button\|menuitem\|option] / [contenteditable] / focusable, excludes tabindex=-1 — accepts a structural `{matches,closest}` so it's DOM-free testable) and the pure `shouldLetNativeCopyRun({boundAction,hasTimelineSelection})` copy-decision. Mirrors the existing `isTypableDOMElement`. Additive exports; existing fns untouched. | 2026-06-18 | Three additive exports; predicate takes a structural element |
| `apps/web/src/actions/use-keybindings.ts` | U3: capture-phase handler now (a) bails on IME (`isComposing`/keyCode 229), (b) returns WITHOUT preventDefault for a bare key while a natively-activatable control is focused (native Space/Enter/type-ahead survives; modifier combos still fire), (c) skips held-key auto-repeat for non-repeat-safe actions, (d) lets native Ctrl+C run when nothing is selected on the timeline (mirrors the paste-copied empty-bail), (e) only preventDefaults Ctrl+R/open-speed-panel when an element is selected (it no-ops otherwise, so it stopped eating browser reload). Added `isBareKeyEvent` helper. | 2026-06-18 | Pure decisions live in browser.ts + repeat-policy.ts; handler wires them |
| `apps/web/src/actions/use-keyboard-shortcuts-help.ts` | U3: removed the bogus `.replace("-", "+")` in `formatKey` (combos already use `+` as separator, so it only corrupted the literal `-` zoom-out key into `+`, which `split("+")` rendered as two empty chips) and exported `formatKey` for unit tests. `formatKey({key:"-"})` now returns `"-"`. | 2026-06-18 | One `.replace` dropped + export keyword |
| `apps/web/src/actions/components/shortcuts-dialog.tsx` | U3 defense-in-depth: `ShortcutItem` now does `key.split("+").filter(Boolean)` so any stray empty segment never renders an empty chip. | 2026-06-18 | One `.filter(Boolean)` |
| `apps/web/src/preview/controllers/preview-interaction-controller.ts` | U4 canvas hover cursor: added a `hoverCursor` (`default`/`move`/`text`) computed on idle pointer-move via the existing `hitTest` — `move` over a selectable element, `text` over a TEXT element (hinting double-click-to-edit). Subscribers re-render on change; reset to `default` on gesture start / off-canvas / mask-edit mode. | 2026-06-18 | New `CanvasHoverCursor` type + `hoverCursor` getter + `updateHoverCursor`; idle branch in onPointerMove |
| `apps/web/src/preview/hooks/use-preview-interaction.ts` | U4: surface the controller's `hoverCursor` through the hook's return value so the overlay can read it. | 2026-06-18 | One returned field |
| `apps/web/src/preview/components/preview-interaction-overlay.tsx` | U4: the canvas application surface reads `hoverCursor` and applies it as the `cursor` style — pan (grabbing/canPan) still takes precedence; `default` falls through to `undefined`. | 2026-06-18 | One ternary in the surface `style.cursor` |
| `apps/web/src/timeline/components/timeline-element.tsx` | U4 clip grab affordance: the clip-body `<button>` in `ElementInner` gained `cursor-grab` + a faint `hover:brightness-110` (distinct from the blue selection-ring boxShadow); resize-handle cursors (`w-resize`/`e-resize`) untouched. | 2026-06-18 | Three utility classes on one button |
| `apps/web/src/timeline/controllers/element-interaction-controller.ts` | U4 global gesture cursor: pins `document.body` cursor to `grabbing` (+ `userSelect:none`) for the drag's lifetime via the new `lockGestureCursor` helper — acquired in `beginDragFromPending`, released in `finishSession` (the single funnel for commit/cancel/threshold-abort, and via destroy→cancel). Kills the mid-drag flicker when the pointer leaves the clip rect. | 2026-06-18 | `cursorLock` field; lock in beginDragFromPending, release in finishSession |
| `apps/web/src/timeline/controllers/resize-controller.ts` | U4 global gesture cursor: pins `document.body` cursor to `ew-resize` (+ `userSelect:none`) for the resize's lifetime via `lockGestureCursor` — acquired in `onResizeStart`, released in `finishSession` AND `destroy` (destroy bypasses finishSession). | 2026-06-18 | `cursorLock` field; lock in onResizeStart, release in finishSession + destroy |
| `apps/web/src/timeline/components/timeline-track.tsx` | U4 forward-tool cursor: when the Track-Select-Forward tool is armed (`isForwardTool`), the track-content root gets `cursor-e-resize` so the tracks reflect the mode. Imported `cn`. | 2026-06-18 | One conditional class + cn import |
| `apps/web/src/preview/components/index.tsx` | U5 handle viewport-clip fix: the `viewportRef` container no longer `overflow-hidden`s the interaction/handle overlay. The scene (canvas mount + AI overlay) moved into its OWN `overflow-hidden absolute inset-0` wrapper so the canvas/letterbox stay clipped when zoomed; the handle overlay sits in a bounded-headroom escape (outer wrapper extends `HANDLE_OVERLAY_HEADROOM_PX`=36 past top/bottom and clips there, inner wrapper re-aligns to the exact viewport box) so a full-bleed element's rotation + top-corner handles stay grabbable without escaping into adjacent panels. Coordinate mapping unchanged. | 2026-06-18 | New const + two wrapper divs; chose bounded headroom over lift-out to preserve the canvas->overlay coordinate origin (anchored to viewportRef) |
| `apps/web/src/preview/components/transform-handles.tsx` | U5: relaxed the handle-root `overflow-hidden` → `overflow-visible` so it doesn't re-clip handles at the element bounds (the bounded-headroom escape in index.tsx now governs the clip). | 2026-06-18 | One class |
| `apps/web/src/preview/components/mask-handles.tsx` | U5: same `overflow-hidden` → `overflow-visible` on the mask-handle root as transform-handles. | 2026-06-18 | One class |
| `apps/web/src/timeline/components/timeline-element.tsx` | U5 narrow-clip resize guard: below `NARROW_CLIP_WIDTH_PX`=16px the two 8px edge handles consumed the whole body and blocked grabbing the clip to MOVE; narrow clips now drop the LEFT handle and render a single thin (4px, edge-flush) RIGHT handle so a central move zone always remains. Threads `isNarrowClip` (= `elementWidth < threshold`) through `ElementInner`→`ResizeHandle`. Normal clips unchanged. | 2026-06-18 | const + isNarrowClip prop on two components |
| `apps/web/src/preview/components/text-edit-overlay.tsx` | U5 text-resize discoverability (anchor, KTD-5): added an edit-mode affordance — a dashed primary ring around the caret box + a counter-scaled "Esc or click away to resize" hint — so the user knows to LEAVE caret-edit mode to reach the (intentionally hidden) resize handles. Does NOT force handles during edit (the modal pattern is intended per the audit). | 2026-06-18 | Ring + hint divs in the scaled wrapper; hint counter-scaled by 1/(scaleX·displayScaleX) for legibility. SEE TO-VERIFY: live-repro gated |
| `apps/web/src/preview/components/toolbar.tsx` | U6 a11y: `aria-label` on the icon-only Play/Pause (state-dependent "Play"/"Pause") and Fullscreen ("Toggle fullscreen") buttons. | 2026-06-18 | Two `aria-label`s |
| `apps/web/src/timeline/components/timeline-toolbar.tsx` | U6 a11y: threaded `aria-label={tooltip}` + `aria-pressed={isActive}` through the shared `ToolbarButton` onto its `Button` (momentary buttons pass `isActive=undefined`, so `aria-pressed` is omitted — only the real toggles announce pressed state); `aria-label` on the icon-only zoom in/out buttons. | 2026-06-18 | Two attrs on ToolbarButton's Button + two zoom aria-labels |
| `apps/web/src/components/editor/panels/assets/views/assets.tsx` | U6 a11y: `aria-label` on the three icon-only MediaActions buttons (Assemble / grid-list view toggle / sort) — they had tooltip content but no accessible name. | 2026-06-18 | Three `aria-label`s |
| `apps/web/src/timeline/components/timeline-ruler.tsx` | U6 a11y: DROPPED the ruler's `role="slider"`/`tabIndex`/`aria-valuemin/max/now`/empty `onKeyDown` — it advertised a slider but reported a frozen `aria-valuenow=0`. The real slider is the playhead (`timeline-playhead.tsx`, live valuenow + arrow-key seek). Added a targeted `eslint-disable` (matching the repo's spatial-gesture-surface convention) since the bare scrub div now trips `jsx-a11y/no-static-element-interactions`. | 2026-06-18 | Removed 5 attrs + onKeyDown; one eslint-disable comment with rationale |
| `apps/web/src/actions/definitions.ts` | U6 keyboard nudge: new `nudge-selected-left`/`nudge-selected-right` actions (editing) bound to `alt+left`/`alt+right` — move the selection one frame. | 2026-06-18 | Two action defs + two default-shortcut rows |
| `apps/web/src/actions/use-editor-actions.ts` | U6 keyboard nudge handlers: build a `MoveGroup` from the selection (anchor = first selected) and commit a ±one-frame offset via `editor.timeline.moveElements`, REUSING `resolveGroupMove` (existing-track target = anchor's own track) so collision/track rules match a mouse drag; selection follows the moved elements. One-frame delta from the new pure `frameOffsetTicks`. | 2026-06-18 | New `nudgeSelected(direction)` + two handlers; imports buildMoveGroup/resolveGroupMove + frame-offset |
| `apps/web/src/actions/use-keyboard-shortcuts-help.ts` | U6: the Hotkeys editor list now seeds from ALL `ACTIONS` (filtered to `isActionWithOptionalArgs`) before overlaying bound keys, so actions shipped WITHOUT a default shortcut (`stop-playback`, `toggle-ripple-editing`) appear as "Not set" and can be bound — it previously iterated bound keys only and hid them. | 2026-06-18 | One seeding loop; `ShortcutItem` already rendered the "Not set" record button |
| `apps/web/src/components/theme-toggle.tsx` | U7 visual polish: the theme icon was hard-coded to `Sun03Icon` in both modes (only the sr-only label flipped). Now renders `Sun03Icon` in dark mode / `Moon02Icon` in light mode — the icon for the mode you'd switch TO, matching the existing sr-only label. | 2026-06-18 | One ternary on the icon + one import |
| `apps/web/src/components/ui/tooltip.tsx` | U7 visual polish: fixed an invalid Tailwind class on the `promotions` tooltip variant — `text-redb-900` → `text-red-900` (the typo'd utility was silently dropped, leaving no text color in light mode). | 2026-06-18 | One-char typo fix |
| `apps/web/src/timeline/components/timeline-element.tsx` | U7 visual polish: the "toggle source audio" context-menu item's icon ternary had BOTH branches as `ScissorIcon` (frozen). Imported `Link02Icon`/`Unlink02Icon` and wired `isElementSourceAudioSeparated ? Unlink02Icon : Link02Icon` (kept the `HugeiconsIcon` wrapper) so the icon reflects linked/separated state — mirrors the sibling toolbar's link/unlink convention. | 2026-06-18 | Two icon imports + one ternary |
| `apps/web/src/components/editor/panels/assets/views/assets.tsx` | U7 visual polish: the bin context-menu "Export clips" item had no handler and wasn't disabled (silent no-op). Marked it `disabled` to match the existing unimplemented-item convention ("Replace media" in timeline-element.tsx). | 2026-06-18 | One `disabled` attr |
| `apps/web/src/components/editor/panels/properties/components/property-param-field.tsx` | U7 visual polish: font params (`param.type === "font"`) now render the app's full `FontPicker` (Google Fonts + system + search, used in masks-tab) instead of the hardcoded 12-font `COMMON_FONTS` `Select`. Wired `FontPicker`'s `defaultValue`/`onValueChange` to the existing onPreview+onCommit; removed the now-dead `COMMON_FONTS` const. (Supersedes the 2026-06-11 "common-system-fonts dropdown" patch on this file.) | 2026-06-18 | Swapped one branch + removed dead const + one import |
| `apps/web/src/components/editable-timecode.tsx` | U7 low-nit: clicking away (blur) from an invalid timecode entry no longer trapped the field in the red error state with Escape as the only exit. `handleBlur` now parses first: a valid entry commits, an invalid one is discarded (revert to the displayed time + exit edit mode). Enter on invalid input still surfaces the inline error. | 2026-06-18 | One guarded branch in `handleBlur` |
